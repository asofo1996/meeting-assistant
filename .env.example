# Copy this file to `.env` and fill in your credentials
# OPENAI_API_KEY is required to generate GPT suggestions
OPENAI_API_KEY=your-openai-api-key
# Optional database settings for PostgreSQL (used when deploying)
DB_USER=
DB_PASS=
DB_NAME=
INSTANCE_CONNECTION_NAME=
